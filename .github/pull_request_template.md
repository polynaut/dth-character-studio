## What & why

<!-- What does this change and why? Link any issue. -->

## Checklist

- [ ] Added a changeset (`pnpm changeset`) if this affects the app/library behavior
- [ ] `pnpm -r typecheck` and `pnpm -r test` pass
- [ ] For desktop/native changes: built/ran via `pnpm dev:desktop`
