---
"@dth/ui": patch
"@dth/web": patch
---

InfoPopup gains a `size` option ('xs' default, 'sm'/'md' matching the kit
Button heights) so an "i" beside a button can match its height — used by
"Import from CSV".
