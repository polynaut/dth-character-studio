---
"@dth/web": patch
---

In the character editor, **Save** now also (re)generates all DTH files in the same step — the separate "Generate DTH files" button is gone. Save is the primary action, and a new **Discard** button reverts unsaved changes (enabled only when there are changes).
