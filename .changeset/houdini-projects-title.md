---
'@dth/web': patch
---

fix(web): declutter the Houdini projects section — the title gets the same size as "Daz scenes" (the two headers stack in the scenes tab and read as one pile at label size), and the Houdini project folder input moves back into the Export directory panel, next to the layout it actually configures. Its per-scene override behaviour is unchanged.
