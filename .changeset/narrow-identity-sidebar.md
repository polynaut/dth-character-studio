---
"@dth/web": patch
---

The character editor's identity sidebar is a step narrower at the xl breakpoint
(28rem, was 32rem), so two Daz-scene cards fit side by side instead of wrapping
on ~1280px-wide windows.
