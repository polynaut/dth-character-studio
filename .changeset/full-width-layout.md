---
"@dth/web": patch
---

Use the full app-window width on every page (desktop layout) instead of a centered narrow column. Character and pose-preset grids gain columns on wide windows to use the space; forms and settings stay at a comfortable reading width, left-aligned. The character "Storage location" panel is restructured into clear labeled fields (full path / folder in library) so it reads cleanly at any width.
