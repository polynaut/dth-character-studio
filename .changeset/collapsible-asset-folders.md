---
'@dth/web': minor
---

**Install Daz assets** report: each source folder is now a collapsible section. The folder header row (with an asset count) toggles its group of asset rows, so long multi-folder scan reports can be skimmed folder by folder; groups start expanded, and the per-asset "files to copy" expansion works as before. Reports without folder headers (DTH release/plugin installs, morphs, Houdini presets) render unchanged.
