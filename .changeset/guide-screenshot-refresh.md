---
---

Docs/CI-only: refresh the guide screenshots and repair the two guide-screenshot tests that `pnpm screenshots` had broken (the `rom-override-toggle` shot waited on the banner removed in #465; `character-scene-tag`'s from-top clip went out of bounds after the outfit-scene selection scrolls the page). No package changes.
