---
'@dth/web': patch
---

Unlinking an Unreal project now pauses on a confirm dialog (same recipe as removing a Daz scene), and the docs-site lightbox fits the viewport on phones — capped to the screen width with a margin instead of overflowing into a sideways scroll.
