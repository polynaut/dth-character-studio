---
---

Test-infrastructure and agent-docs only, no product change: `SMOKE_PORT` keeps a
second checkout's smoke server from silently answering this one's run, and the
prebuilt bundle's real effect on `updater.ts` is recorded where it can be acted
on.
