---
'@dth/web': patch
'@dth/ui': patch
---

fix(web,ui): every modal dialog's **Cancel** button moves from the far left into the right-aligned button group, always as its first item — one footer layout across the app.
