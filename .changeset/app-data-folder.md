---
"@dth/web": minor
---

Show the app data folder in General settings — a read-only path chip pointing at where the app keeps its settings, project list, pose catalog and avatar images, so it's easy to find and back up.
