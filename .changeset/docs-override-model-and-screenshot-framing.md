---
---

docs: rewrite the multiple-Daz-scenes / per-scene override guide for the shipped
implicit edit-to-override model (no toggle; one ROM script auto-selects the open
scene; per-scene `_pose_asset.csv`), fix the stale per-scene-`.dsa` claims, and
regenerate every guide screenshot. Screenshot framing is now dynamic — the shots
drop the app's sticky chrome and scroll the feature to the top with a
measure-and-correct loop, so a UI restyle can't tuck a feature's title under a
header (no more hand-tuned crop offsets).
