---
'@dth/web': patch
---

fix(web): the Settings/Tools sticky header is now opaque — the frosted-glass translucency that let form content shimmer through under the title is gone (matching the character editor's header).
