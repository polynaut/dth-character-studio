---
"@dth/web": patch
---

Restyle the default toggle switches with a neumorphic look: a recessed pill track
(inset shadow) cradling a raised, softly-domed knob (drop shadow + gradient). The
green override toggles are unchanged.
