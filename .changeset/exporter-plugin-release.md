---
"@dth/web": minor
---

Select a DTH Exporter Plugin release in Settings — point at the plugin folder (or a folder of versioned plugin folders) and the version is read straight from the exporter DLL.
