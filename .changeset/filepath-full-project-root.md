---
"@dth/web": patch
---

The editor's Filepath prefix chip now shows the full project root path (e.g.
`X:\_3d\dth-characters\`) instead of the `\project\` placeholder, now that the
field spans the full width.
