---
"@dth/desktop": patch
---

New dark macOS app icon — the logo now sits on a slate rounded background instead of a light one, so it reads cleanly in the Dock in both light and dark mode. The Windows icon is unchanged.
