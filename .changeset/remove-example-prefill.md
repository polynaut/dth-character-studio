---
'@dth/web': patch
---

**Removed the "Example" ROM prefill.** New characters start Empty or prefill
from one of your own characters (any project) — the bundled example character
is gone from the create panel, the API and the guide.
