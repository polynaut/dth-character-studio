---
---

Guide-only refresh audit, no product change: the Houdini card's warning list
gains the two checks it had grown since it was last written (`$HIP/../…` paths
and missing baker textures), and the note under it stops claiming that material
texture paths are never verified — they are, scoped to the bakers' layer
textures. Also: **Make paths portable** now says it re-homes those pre-v0.69
`$HIP/../…` paths, the General tab's **PoseAsset CSV path** row is described,
the DTH Export dialog's Unreal section no longer says a send takes *every*
export set when the sets are ticked one by one, and an out-of-date Unreal Runner
is documented as refusing a send rather than only colouring the card amber.
Screenshots regenerated.
