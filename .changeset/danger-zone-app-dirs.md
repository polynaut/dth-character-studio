---
'@dth/desktop': patch
---

Danger zone (uninstall-Daz cleanup): the "Prefill folder paths" defaults now also include the Daz Studio app install folders `C:\Program Files\DAZ 3D\DAZStudio6` and `C:\Program Files\DAZ 3D\DAZStudio4` (existing ones only, as before), so a full cleanup can also remove the application itself — not just its content/library folders.
