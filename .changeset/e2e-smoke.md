---
---

CI/tooling only — a Playwright browser smoke suite (`pnpm --filter @dth/web e2e`): the real SPA in a real browser against an in-memory fake of the native Tauri layer, covering launcher → project → character editor → Save writing both generated artifacts. No product code changes.
