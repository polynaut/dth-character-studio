---
'@dth/web': patch
---

Kill the last 1px layout shift between an empty group and its first morph row — the placeholder now mirrors the name input's exact vertical metrics instead of a hard-coded height.
