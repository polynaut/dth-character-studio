---
'@dth/web': patch
---

The character page's sticky header now has a liquid-glass background — a translucent fill with a heavy backdrop blur, so ROM content scrolling beneath frosts through it, echoing the native macOS title bar above. Falls back to the opaque background where `backdrop-filter` isn't supported.
