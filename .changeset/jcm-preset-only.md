---
"@dth/web": patch
---

JCM (Joint Corrective) is preset-only now — the Mode picker is hidden for it
(like Retargeting), since the base Joint Corrective ROM always comes from a
pre-defined DTH asset.
