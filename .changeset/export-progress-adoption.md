---
'@dth/web': patch
---

fix(web): the DTH Export button shows the progress of ANY live Runner batch — including a scene card's ROM-animation generate and runs started in another window (previously only the run its own Start click armed). Display only: outcome toasts stay with the window that started the run.
