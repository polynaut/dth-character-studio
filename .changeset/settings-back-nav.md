---
'@dth/web': patch
---

Settings: the back link now returns you to wherever you opened Settings from (popping history, like the About page) instead of always jumping to the projects list — and names the destination (e.g. "Back to Kira") when you entered from a character page.
