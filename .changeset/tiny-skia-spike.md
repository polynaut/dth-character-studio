---
---

Dev-only spike: a `tiny-skia` example evaluating anti-aliased vector rendering
(`apps/desktop/examples/tiny_skia_demo.rs`). tiny-skia is a dev-dependency, so it
links only into the example — the shipped app is unchanged. No product change.
