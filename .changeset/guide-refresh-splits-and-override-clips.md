---
---

Docs only: the guide catches up with what shipped, and stops saying three things
that are no longer true.

Documented: a DTH Export that produced nothing is now reported as a failure
rather than a success (and what the runtime-load report says it found); the
per-character product-scan script's Content Library tile, and what the Daz
Products toggle really switches off.

Corrected: a DTH Export **opens Daz where you can see it** — the guide still
said a closed Daz starts minimized, which is now true only of unattended scans;
the Houdini card's busy bar is a moving stripe pattern and **keeps moving under
reduced motion**, where the guide described the old single glint and claimed
reduced motion held it still.

Three pages were doing more than one job and are split, with every inbound link
repointed: the DTH Export batch and the two bundled fix-it scripts leave
"Build the ROM in Daz Studio" (561 -> 239 lines), and the Utils drawer's project
checks leave the transfer page (565 -> 271). No content dropped.

Also three new interaction clips for per-scene overrides, which is a mechanism
with no switch to photograph: selecting a non-primary scene making the cube
glyphs appear, a field turning green as it is edited while its neighbour stays
inherited, and the green cube becoming the reset that undoes it.
