node_modules
.DS_Store
dist
dist-ssr
*.local
.env
todos.json

# Personal character data + generated DTH files (repo is public)
data/

.claude/worktrees/

# Playwright (e2e) run output
test-results/
playwright-report/

# TanStack Router plugin temp output
.tanstack

# Generated at deploy time by scripts/build-guide-site.mjs (pages.yml)
site/guide/

# Staged at build time by scripts/fetch-runner.mjs (bundled Runner plugin DLLs)
apps/desktop/resources/dth-runner/

# Python bytecode (a stray py_compile of 456.py)
__pycache__/

# Dev-server output when `pnpm dev:desktop` is redirected to a file (detached
# runs need a log somewhere, and the repo root is the convenient place).
*.log
